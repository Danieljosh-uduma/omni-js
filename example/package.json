{
  "name": "example",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "predev": "node -e \"require('fs').copyFileSync('../packages/runtime/dist/omni-runtime.js', './omni-runtime.js')\"",
    "dev": "npx serve .",
    "build": "omni build src/App.omni --outdir=dist",
    "start": "npx serve dist"
  },
  "dependencies": {
    "@omni/runtime": "*",
    "@omni/cli": "*",
    "gsap": "^3.12.5"
  }
}
